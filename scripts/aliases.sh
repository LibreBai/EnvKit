#!/bin/bash

#-------- docker 相关操作， do 开头 --------#
alias docl="sudo docker container ls -a"
alias docr="sudo docker container rm -f "
alias doil="sudo docker image ls -a"
alias doir="sudo docker image rm -f "

alias dos="sudo docker search "
alias dop="sudo docker pull "